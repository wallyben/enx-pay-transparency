/**
 * How a comparable category was derived (S09). Equal-value and overrides are S10+.
 */
export enum CategoryAssignmentBasis {
  EXACT = 'EXACT',
  NORMALIZED_EQUIVALENT = 'NORMALIZED_EQUIVALENT',
}
