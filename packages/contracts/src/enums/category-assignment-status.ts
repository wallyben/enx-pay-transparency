/**
 * Outcome of comparable category assignment (S09). First-class states for gating downstream metrics (S11+).
 */
export enum CategoryAssignmentStatus {
  ASSIGNED = 'ASSIGNED',
  REVIEW_REQUIRED = 'REVIEW_REQUIRED',
  UNASSIGNED = 'UNASSIGNED',
}
