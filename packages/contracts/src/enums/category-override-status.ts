/**
 * Lifecycle of a governed category override proposal (S10). Distinct from row assignment status.
 */
export enum CategoryOverrideStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
}
